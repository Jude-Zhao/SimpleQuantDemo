"""Built-in factor implementations.

Each factor is a self-contained module registered via ``@register_factor``.
New factors can be added here or in a separate package; the registry
auto-discovers them on startup.
"""