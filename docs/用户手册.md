# SimpleQuantDemo 用户手册

本手册面向量化研究员，说明如何运行当前系统、查看结果、调整参数，以及如何基于系统做 ETF 周度轮动投研。

## 1. 系统能做什么

SimpleQuantDemo 是一个本地数据驱动的多因子 ETF 周度轮动系统。当前版本包含两个入口：

| 系统 | 命令入口 | 用途 |
|------|----------|------|
| 投研系统 | `python -m research.main` | 读取本地数据，构建因子，计算 IC/ICIR，合成因子，回测，输出图表和统计 |
| 实盘系统 | `python -m trading.main` | 基于最新收盘数据生成最新持仓 CSV |

当前内置两个价格因子：

| 因子 | 默认参数 | 含义 |
|------|----------|------|
| 动量因子 | 5日收益率 | 近期上涨越多，因子值越高 |
| 波动率因子 | 20日年化波动率 | 近期波动越大，因子值越高 |

系统也支持用户自己写 Python 因子，包括宏观映射因子。

## 2. 开发环境

后续运行默认使用以下 Conda 环境：

| 项目 | 值 |
|------|----|
| 环境名 | `QuantitativeTrading` |
| Python路径 | `D:\Coding\APPS\Miniconda3Py38_4.9.2\envs\QuantitativeTrading\python.exe` |
| Python版本 | 3.10.14 |

建议所有命令都在项目根目录运行：

```powershell
cd D:\Coding\Workplace\TraeWorkplace\SimpleQuantDemo
```

验证 Python：

```powershell
& 'D:\Coding\APPS\Miniconda3Py38_4.9.2\envs\QuantitativeTrading\python.exe' --version
```

验证测试环境：

```powershell
& 'D:\Coding\APPS\Miniconda3Py38_4.9.2\envs\QuantitativeTrading\python.exe' -m pytest -q
```

如果看到类似下面输出，说明基础环境正常：

```text
59 passed
```

## 3. 项目目录

重要目录如下：

```text
core/                 公共核心逻辑
  data/               数据源、数据清洗、数据校验
  factors/            因子接口、动量、波动率
  analysis/           IC、RankIC、ICIR、共线性
  synthesis/          ICIR半衰期加权合成
  optimization/       Top N等权组合优化
  calendar.py         调仓日生成

research/             投研系统
  config.py           投研配置
  main.py             投研入口
  backtest.py         回测引擎
  visualization/      图表输出

trading/              实盘信号系统
  config.py           实盘配置
  main.py             实盘入口
  signal.py           持仓信号生成

data_example/         样例数据
docs/                 文档
tests/                自动化测试
```

## 4. 数据说明

当前系统默认使用 `data_example/` 下的数据：

| 文件 | 内容 |
|------|------|
| `etf量价数据_1.csv` | 28只ETF日频行情，字段包括 date/sec/open/high/low/close/volume/amount |
| `高频经济指标_1.csv` | 日频宏观指标宽表 |
| `附件1 28只非债券ETF产品池.xlsx` | ETF产品池 |

数据读取层会自动处理：

1. 混合日期格式，例如 `2026-03-13` 和 `2026/3/13`。
2. ETF 行情长表格式。
3. 宏观指标宽表格式。
4. 产品池与行情证券代码是否一致。
5. 宏观指标按交易日对齐并前向填充。

## 5. 运行投研系统

在项目根目录运行：

```powershell
& 'D:\Coding\APPS\Miniconda3Py38_4.9.2\envs\QuantitativeTrading\python.exe' -m research.main
```

也可以指定回测区间：

```powershell
& 'D:\Coding\APPS\Miniconda3Py38_4.9.2\envs\QuantitativeTrading\python.exe' -m research.main --start-date 2025-06-02 --end-date 2026-03-13
```

也可以指定输出目录：

```powershell
& 'D:\Coding\APPS\Miniconda3Py38_4.9.2\envs\QuantitativeTrading\python.exe' -m research.main --start-date 2025-06-02 --end-date 2026-03-13 --output-dir .tmp_system_test\research
```

默认输出目录：

```text
research/output/backtest_results/
```

## 6. 投研输出怎么看

投研系统会输出以下文件：

| 文件 | 说明 |
|------|------|
| `summary.csv` | 回测统计指标 |
| `equity_curve.csv` | 每日策略净值 |
| `weights.csv` | 每日持仓权重 |
| `factor_stats.csv` | IC、RankIC、ICIR 序列 |
| `synthesized_scores.csv` | 合成因子分数 |
| `warnings.txt` | 共线性警告和被剔除因子 |
| `equity_curve.png` | 净值曲线图 |
| `factor_stats.png` | IC、RankIC、ICIR 图 |
| `latest_weights.png` | 最新持仓权重图 |

### 6.1 `summary.csv`

核心字段：

| 字段 | 含义 |
|------|------|
| `total_return` | 区间总收益 |
| `annual_return` | 年化收益 |
| `annual_volatility` | 年化波动率 |
| `sharpe` | 夏普比率 |
| `max_drawdown` | 最大回撤 |
| `win_rate` | 日胜率 |
| `turnover_sum` | 累计换手 |
| `cost_sum` | 累计交易成本 |
| `rebalance_count` | 调仓次数 |

### 6.2 `weights.csv`

每一行是一个交易日，每一列是一只 ETF。权重大于 0 的列就是当天持仓。

检查最新持仓可以用：

```powershell
Get-Content research\output\backtest_results\weights.csv -Tail 5
```

### 6.3 `factor_stats.csv`

这个文件包含三类指标：

1. `ic`：因子值与未来 5 日收益的横截面 Pearson 相关。
2. `rank_ic`：因子排名与未来 5 日收益排名的 Spearman 相关。
3. `icir`：滚动 IC 均值除以滚动 IC 标准差。

当前研究入口默认只在调仓日样本上统计 IC/RankIC/ICIR。

## 7. 运行实盘信号系统

实盘系统只输出最新持仓，不做回测。

运行：

```powershell
& 'D:\Coding\APPS\Miniconda3Py38_4.9.2\envs\QuantitativeTrading\python.exe' -m trading.main
```

指定数据区间：

```powershell
& 'D:\Coding\APPS\Miniconda3Py38_4.9.2\envs\QuantitativeTrading\python.exe' -m trading.main --start-date 2025-06-02 --end-date 2026-03-13
```

指定输出目录：

```powershell
& 'D:\Coding\APPS\Miniconda3Py38_4.9.2\envs\QuantitativeTrading\python.exe' -m trading.main --start-date 2025-06-02 --end-date 2026-03-13 --output-dir .tmp_system_test\trading
```

默认输出目录：

```text
trading/output/positions/
```

输出文件命名：

```text
position_YYYYMMDD.csv
```

示例：

```csv
sec,weight
513880.SH,0.2
512680.SH,0.2
512400.SH,0.2
512980.SH,0.2
515880.SH,0.2
```

## 8. 如何调整参数

当前参数在 Python 配置类里。

投研配置位于：

```text
research/config.py
```

实盘配置位于：

```text
trading/config.py
```

常用参数：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `start_date` | `2019-11-01` | 起始日期 |
| `end_date` | `None` | 结束日期，None 表示用到数据末尾 |
| `momentum_window` | 5 | 动量因子窗口 |
| `volatility_window` | 20 | 波动率因子窗口 |
| `forward_return_horizon` | 5 | IC 使用未来 N 日收益 |
| `ic_min_periods` | 10 | 横截面最少有效 ETF 数 |
| `icir_window` | 20 | ICIR 滚动窗口 |
| `icir_min_periods` | 10 | ICIR 最少样本 |
| `half_life_periods` | 20 | ICIR 半衰期 |
| `collinearity_threshold` | 0.7 | 共线性阈值 |
| `top_n` | 5 | 持仓数量 |
| `max_weight` | 0.5 | 单个标的权重上限 |
| `transaction_cost_bps` | 5 | 回测交易费率，单位 bps |

如果只是临时改区间，优先用 CLI 参数 `--start-date` 和 `--end-date`。

如果要改因子窗口、Top N、交易成本，建议在配置文件里改。

## 9. 如何做一次完整投研

建议流程：

1. 更新或确认本地数据。
2. 运行全量测试。
3. 跑一遍投研系统。
4. 查看统计表和图表。
5. 判断因子表现。
6. 修改参数或新增因子。
7. 重复回测。
8. 用实盘系统生成最新持仓。

### 9.1 运行测试

```powershell
& 'D:\Coding\APPS\Miniconda3Py38_4.9.2\envs\QuantitativeTrading\python.exe' -m pytest -q
```

### 9.2 运行回测

```powershell
& 'D:\Coding\APPS\Miniconda3Py38_4.9.2\envs\QuantitativeTrading\python.exe' -m research.main --start-date 2025-06-02 --end-date 2026-03-13
```

### 9.3 查看输出

打开：

```text
research/output/backtest_results/summary.csv
research/output/backtest_results/equity_curve.png
research/output/backtest_results/factor_stats.png
research/output/backtest_results/latest_weights.png
```

重点看：

1. `total_return` 是否为正。
2. `max_drawdown` 是否可接受。
3. `sharpe` 是否稳定。
4. `factor_stats.png` 里 IC/RankIC 是否长期偏正。
5. `warnings.txt` 是否提示因子高度相关。
6. `weights.csv` 是否存在异常集中持仓或长期不换仓。

### 9.4 生成最新持仓

```powershell
& 'D:\Coding\APPS\Miniconda3Py38_4.9.2\envs\QuantitativeTrading\python.exe' -m trading.main
```

查看：

```text
trading/output/positions/position_YYYYMMDD.csv
```

## 10. 如何新增一个自定义因子

系统要求因子输出为：

```python
DataFrame(index=date, columns=sec, values=factor_value)
```

最简单的方式是在研究入口里构造一个新的 DataFrame，并放进 `factor_panel`。

例如，一个简单的 10 日反转因子：

```python
from core.factors.utils import pivot_price_field

close = pivot_price_field(price_data, field="close", universe=universe)
reversal_10 = -close.pct_change(10, fill_method=None)

factor_panel = {
    "momentum_5": momentum_factor,
    "volatility_20": volatility_factor,
    "reversal_10": reversal_10,
}
```

如果要写成标准类，可以继承：

```python
from core.factors.base import FactorBuilder
from core.factors.utils import pivot_price_field, validate_factor_matrix

class ReversalFactor(FactorBuilder):
    def __init__(self, window: int = 10):
        self.window = window

    @property
    def name(self) -> str:
        return f"reversal_{self.window}"

    def build(self, price_data, macro_data, universe):
        close = pivot_price_field(price_data, field="close", universe=universe)
        factor = -close.pct_change(self.window, fill_method=None)
        validate_factor_matrix(factor, universe, name=self.name)
        return factor
```

## 11. 如何写宏观映射因子

当前系统不内置固定宏观映射模型。用户可以自己把宏观数据映射为 `date × ETF` 因子矩阵。

示意：

```python
from core.factors.utils import pivot_price_field

close = pivot_price_field(price_data, field="close", universe=universe)
macro_signal = macro_data["汇率：美元指数"].pct_change(20)

# 示例逻辑：美元指数上涨时偏向黄金ETF，否则偏向权益ETF
factor = close.copy() * 0.0
factor.loc[:, :] = -macro_signal.reindex(factor.index).values.reshape(-1, 1)
factor["518880.SH"] = macro_signal.reindex(factor.index)
```

注意事项：

1. 宏观数据是否滞后由用户因子自行处理。
2. 不要在因子里使用未来数据。
3. 输出列必须与 `universe` 顺序一致。
4. 上市前或无行情阶段可以保留 NaN。

## 12. 当前限制

当前版本是本地样例数据驱动的 V1 Demo，有以下限制：

1. AkShare 实时抓取尚未完整实现。
2. 暂无独立基准净值曲线。
3. 暂无因子相关性热力图。
4. 参数主要通过 Python 配置修改，尚未支持 YAML/JSON。
5. 宏观映射因子需要用户自行编写。
6. 图表是基础版本，未做复杂排版。

## 13. 常见问题

### 13.1 为什么运行后结果文件没有进入 Git？

`research/output/` 和 `trading/output/` 已被 `.gitignore` 忽略。输出结果是运行产物，不建议提交。

### 13.2 为什么实盘系统还会计算 ICIR？

实盘系统需要用历史 ICIR 作为因子合成权重。它不会回测，只用历史统计结果合成最新因子分数。

### 13.3 为什么最后几个交易日 IC 是空的？

IC 使用未来 5 个交易日收益。数据末尾没有未来收益，所以末尾样本不会参与 IC 计算。

### 13.4 为什么交易信号日期不是今天？

系统默认输入数据里的最新日期就是最新收盘数据日期。信号日期等于本地数据中的最新可用交易日。

### 13.5 如何确认当前系统没坏？

运行：

```powershell
& 'D:\Coding\APPS\Miniconda3Py38_4.9.2\envs\QuantitativeTrading\python.exe' -m pytest -q
```

如果全部测试通过，再运行：

```powershell
& 'D:\Coding\APPS\Miniconda3Py38_4.9.2\envs\QuantitativeTrading\python.exe' -m research.main --start-date 2025-06-02 --end-date 2026-03-13
& 'D:\Coding\APPS\Miniconda3Py38_4.9.2\envs\QuantitativeTrading\python.exe' -m trading.main --start-date 2025-06-02 --end-date 2026-03-13
```

